## v0.42.16

- Fixed: creating or editing an activity that synced immediately would still show a pending sync badge — the badge now clears right after the sync completes
- Fixed: the activity date filter now always includes the full current month; the range is anchored to the start of the current month rather than today's date
- Fixed: edit and delete buttons on activity cards now match the styling used on contacts
