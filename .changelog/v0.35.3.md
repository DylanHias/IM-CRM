# v0.35.3

- The start and end fields when adding an activity are now stacked vertically for a cleaner layout
- The time picker now uses a styled dropdown instead of the system default, matching the rest of the app
