v2.12.72
- Fixed: the app now checks for updates the moment it starts, so a broken startup can fix itself once a new version is out — no need to wait for the app to fully load
- Fixed: if the database fails to load, the app automatically checks for an update in the background while you're on the error screen
