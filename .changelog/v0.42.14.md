# v0.42.14

- Pending sync queue on the Sync tab — you can now see all activities and follow-ups waiting to be pushed to Dynamics 365, listed in a table above the sync history. Both tables include pagination so you can browse through large queues.
- Sync history pagination was already present and remains unchanged.
