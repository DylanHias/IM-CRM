# v0.12.17

- The Activities by User chart is now a clean horizontal bar chart without grid lines. It scales its height automatically based on the number of users, so it works well even with 20+ users.
