# v0.18.2

- Fixed an issue where the desktop app could show empty or incomplete data on certain pages (follow-ups, opportunities, admin) when the database was only partially set up
- The app now automatically detects and fills in any missing data when it starts up, so you no longer need to reset anything manually
- All pages in the desktop app now properly show sample data as a fallback when the database is empty
