- Keyboard shortcut badges now show all keys grouped together in one badge (e.g. "Ctrl+Shift+R" as a single badge instead of three separate ones)
- On the keybindings settings page, you now click the pencil icon to change a shortcut instead of clicking on the shortcut badge itself
