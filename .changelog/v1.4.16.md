v1.4.16

- Improved: initial sync is now dramatically faster — contacts, calls, appointments, notes, tasks, and opportunities are all fetched from D365 at the same time instead of one after another
- Improved: records are written to the local database in bulk batches instead of one at a time, cutting thousands of individual write operations down to a handful
- Improved: duplicate database lookups during sync are eliminated by loading existing data once upfront instead of once per record
