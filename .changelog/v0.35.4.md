# v0.35.4

- Sync errors now actually show up in the debug page — partial failures (like individual record push errors) were previously hidden
- Error rows now show a status badge so you can tell full failures from partial ones
