# v0.44.8

## Fixed

- Deleting an opportunity no longer fails when the remote delete needs to be queued for later sync. Previously, the app would crash with a database error because the pending-delete table didn't accept opportunity records.
