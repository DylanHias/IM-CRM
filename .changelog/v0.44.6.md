# v0.44.6

## Changed

- The activity board no longer has separate "Rejected" and "Expired" columns — they've been combined into a single "Cancelled" column for a cleaner overview.
- Status dropdowns throughout the app now show "Cancelled" instead of the old "Rejected" and "Expired" options.
