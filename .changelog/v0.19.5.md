# v0.19.5

- Admin dashboard now always loads data from the database instead of sometimes using placeholder data
- This means all charts and tables (users, activities, audit log, sync status, pipeline) always reflect real information
