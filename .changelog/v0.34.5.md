# v0.34.5

- All date fields now use a proper calendar picker with month and year dropdowns instead of the plain browser date input
- Date and time fields (for meetings and visits) now show a calendar picker alongside a separate time input for easier scheduling
