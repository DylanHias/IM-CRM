# v1.0.12

- Fixed: exporting large customer lists (10,000+) no longer freezes the app
- The save dialog now appears immediately when you click Export — you pick the file location right away while the data is prepared in the background
- A spinner and "Exporting…" label show on the button while the file is being generated
- A toast notification confirms when the export is complete or if something went wrong
