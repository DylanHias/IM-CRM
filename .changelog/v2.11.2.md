v2.11.2

- Fixed: sync no longer fails on records owned by colleagues — when you delete a note, follow-up, or opportunity that someone else owns in Dynamics, the app now skips it cleanly instead of getting stuck retrying forever
