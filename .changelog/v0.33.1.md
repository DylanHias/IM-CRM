# v0.33.1

- Activities are now skipped when their linked contact doesn't exist locally, preventing sync errors. The reason is logged for each skipped record.
- Sync logs now show error counts per entity type for easier debugging.
