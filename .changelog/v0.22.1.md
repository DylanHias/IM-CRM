# v0.22.1

- The "Check for updates" button in settings now lets you install the update right away — after finding an update, the button changes to "Install update" so you can download and install it with one more click.
