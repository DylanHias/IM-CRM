# v0.42.7

Fixed an issue where activities (calls, meetings, visits) would silently fail to sync automatically when saved. They were being sent to the backend with the wrong user ID, causing the sync to reject them. Activities created while this bug was present will be picked up by the next manual sync.
