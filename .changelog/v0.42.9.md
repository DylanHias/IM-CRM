# v0.42.9

- Build now runs automatically before every commit — no more manual build step required
- Improved guidance for parallel task handling to speed up multi-step work
- Library documentation is now looked up from live sources for more accurate results
