# v0.20.1

- Fixed the "Activities Over Time" chart so each line now shows its real count instead of stacking on top of the others. Before, a type with 1 activity could appear at 3 on the chart — now it shows at 1 where it belongs.
