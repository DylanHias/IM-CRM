v2.12.41
- Fixed: Contact Type dropdown on the contact form now actually loads every active contact type from D365 (Cloud Contact, Cisco SBO Contact, Customer Service, Credit, etc.). Each option shows the country code in parentheses so the Belgian and Dutch variants of the same type can be told apart.
