v2.12.6
- Fixed: Opportunities page crashed with "Invalid currency code: US Dollar" because Dynamics returned the localized name instead of the ISO code; the app now stores ISO codes (USD, EUR, …) and falls back gracefully if a code is unknown
