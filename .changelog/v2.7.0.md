v2.7.0

- Added: When ARR data isn't accessible, the Sync page now shows a banner with a one-click "Email request" button — opens a pre-written email asking the Power BI dataset owner to grant Build permission, no copy-pasting required
- Improved: ARR sync now skips silently after the first access-denied error instead of retrying every cycle, and resumes automatically once permission is granted
