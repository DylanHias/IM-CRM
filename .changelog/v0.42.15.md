# v0.42.15

- Fixed: changelog popup was showing extra section headers (like "What's new") and bold formatting — now only the version number appears as a header and text is plain.
- Fixed: activity date filter range now extends to the end of the current month instead of cutting off mid-day.
