v1.9.0

- Pages now fade in smoothly each time you switch between them
- Tab panels in Settings, Analytics, Help, Admin, and Debug now crossfade instead of snapping instantly
- The active tab highlight in Settings, Analytics, Help, Admin, and Debug now slides between items with a spring animation
- Dashboard KPI cards, map, and side panels now fade in sequentially when you open the app
- Follow-Up lists now animate in row by row in the overdue, upcoming, and completed sections
- Invoice rows now slide in when loading; switching customers crossfades the invoice view
- Revenue Overview rows now slide in when changing pages; the title and filter bar fade in on load
- Timeline page title and search bar now fade in on load
- Sync page sections now fade in on load
- Activity and Follow-Up log forms now fade in on load
- Shared animation language extracted into a single module so all effects stay consistent across the app
- Animations are disabled automatically if you have Reduce Motion turned on in your OS settings
