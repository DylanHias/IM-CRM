# v0.17.2

- Fixed the data quality cards (top 3 stats) not showing on the Analytics tab in the desktop app.
- All admin panel sections now handle errors gracefully — if one query fails, the rest still load.
