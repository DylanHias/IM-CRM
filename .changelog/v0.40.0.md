# v0.40.0

- **New: Database Explorer in Admin Panel** — You can now run SQL queries directly from the Admin panel's new "Database" tab. Type any query, hit Execute (or Ctrl+Enter), and see results instantly in a table.
- **Save & reuse queries** — Save frequently used queries by name so you can load them again later with one click. Update or delete saved queries as needed.
