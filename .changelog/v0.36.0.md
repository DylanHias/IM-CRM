# v0.36.0

- You can now choose which columns to show or hide on the Invoices and ARR Overview tables using the new "Columns" button
- Columns can be reordered by dragging them in the column picker — your preferences are saved automatically
- Table headers now stay visible at the top when scrolling through long lists on Invoices and ARR Overview
