# v0.44.7

## Fixed

- Opportunity contact sync now correctly uses `parentcontactid` — the standard D365 field — instead of `contactid` which D365 doesn't recognise on opportunities. Contact links will now sync properly both when reading from and writing to D365.
