# v0.42.5

- Meeting and visit end time now defaults to 1 hour after the start time
- Changing the start time automatically adjusts the end time so it can't be earlier
- You can no longer set an end time before the start time — hours and minutes that would result in an invalid time are greyed out and unclickable
