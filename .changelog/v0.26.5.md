# v0.26.5

- Fixed sign-in failing because Microsoft was blocking the login request from the app's built-in browser
- The login exchange now happens behind the scenes through the app itself, avoiding the block entirely
