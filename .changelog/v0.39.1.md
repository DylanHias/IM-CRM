# v0.39.1

- Added a new "Sync pending interval" setting that controls how often your local changes are automatically pushed to D365 (default: every 15 minutes)
- Changed the default full sync interval from 15 to 30 minutes
