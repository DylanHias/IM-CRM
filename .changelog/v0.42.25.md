# v0.42.25

- Fixed end time showing 00:00 instead of 1 hour after start when adding or editing activities from the customer detail page
- Changing the start time now automatically adjusts the end time if it would end up before the start
- End time can no longer be set before the start time
