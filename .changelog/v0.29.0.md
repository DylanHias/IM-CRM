- Syncing is now much faster — only new or changed data is pulled from D365 instead of everything
- First sync still downloads all your data, but every sync after that only grabs what's changed
- All data updates during sync now happen in one go instead of one-by-one, making it significantly quicker
- Dropdown option syncing now happens all at once instead of waiting for each one to finish
- The app's local storage is now tuned for better performance with larger amounts of data
