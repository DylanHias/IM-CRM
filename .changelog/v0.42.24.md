# v0.42.24

- Fixed end time defaulting to 00:00 instead of 1 hour after the start time when creating a meeting or visit
- Fixed edit modal allowing end time to be set before start time — it now blocks invalid selections and greys out earlier times
- When changing the start time in the edit modal, end time now auto-advances if it would end up before the new start
