# v0.42.11

- Fixed an issue where activities (calls, meetings, visits) created directly from the customer page would fail to sync to D365 due to using a stale or incorrect user ID. They now always use the same identity resolution as the manual sync in the sync tab.
