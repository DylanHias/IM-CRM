- Dropdown choices (like opportunity stage, type, and sell type) now come directly from D365 instead of being fixed in the app — if someone adds a new option in D365, it automatically shows up here
- Customer details now include BCN, segment, cloud customer status, and language pulled from D365
- When logging a meeting or visit, you can now pick a start and end date/time — these are sent to D365 as proper appointment times
- Phone calls now link to the contact in D365 as the "Call To" person
- The vendor field on opportunities is now a free-text field with suggestions from previously used vendors, instead of a fixed list
- Fixed an issue where owner names, business unit names, and user names from D365 weren't showing up correctly
