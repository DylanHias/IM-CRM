# v0.30.6

- Improved error visibility — actions that could fail silently now show proper error messages in the admin console
- Settings pages load slightly faster thanks to shared internal components
- Removed an unused top bar component that was still shipping with the app
- Cleaned up leftover mock data and dead code, reducing the app's bundle size
- Fixed inconsistent log labels across several pages
