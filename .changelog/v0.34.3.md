# v0.34.3

- Fixed an issue where syncing activities and follow-ups from D365 would fail because the database was missing a required column. Existing databases are automatically repaired on next launch.
