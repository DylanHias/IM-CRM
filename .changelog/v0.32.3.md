# v0.32.3

- Contacts are now fetched from D365 with a Benelux country filter at the API level, significantly reducing the amount of data downloaded during sync.
- All child entities (contacts, activities, follow-ups) are filtered in the adapter to only return records for companies in your local database.
