# v1.0.13

- Fixed: exporting large datasets no longer freezes the app — Excel files are now generated natively in Rust, completely off the JavaScript engine, so no matter how many rows you export the interface stays fully responsive
- Removed: the CSV export format option has been removed; all exports are now Excel (.xlsx) only
