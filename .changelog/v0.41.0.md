# v0.41.0

- Activities now show only recent entries by default (last 3 months). Use the date filter to pick a different range or show all.
- Empty activity columns (like Rejected and Expired when they have no items) now collapse to save screen space. Click them to expand, or drag an activity onto them.
- The activity tab count now reflects the filtered number of activities, not the total.
