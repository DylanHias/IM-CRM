# v0.32.2

- Contacts, activities, and follow-ups are now filtered to only sync for companies already in your local database (Benelux scope). Previously, all records from D365 were processed regardless of region, causing unnecessary data and slower syncs.
